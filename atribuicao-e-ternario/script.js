console.log('↓ Retorno do operador ternário');
let idade = 16;
let podeBeber = idade >= 18 ? 'Pode beber' : 'Não pode beber';
console.log(podeBeber);
// condição ? true : false

//Escopo
// Declarar variaveis sem a palavra chave var, const, ou let, cria uma variável que pode acessar em qualquer escopo (global).
//Isso é um erro.
